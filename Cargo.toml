[package]
name = "trellis"
version = "0.1.0"
edition = "2021"
description = "A small CLI task tracker — the Omegon starter project"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
